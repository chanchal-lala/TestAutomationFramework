Quick setup — if you’ve done this kind of thing before
or	
HTTPS
SSH
https://github.com/ckl123/SeleniumJavaFramework.git
Get started by creating a new file or uploading an existing file. We recommend every repository include a README, LICENSE, and .gitignore.

…or create a new repository on the command line
echo "# SeleniumJavaFramework" >> README.md
git init
git config credential.helper store
git remote add origin https://github.com/ckl123/TestAutomationFramework.git
git config --global user.email "chnchlkmrll1@gmail.com"
git config --global user.name "ckl123"
git pull -u origin master
git status
git add README.md or git add --all
git commit -m "first commit"
git push -u origin master
                
…or push an existing repository from the command line
git remote add origin https://github.com/ckl123/SeleniumJavaFramework.git
git push -u origin master